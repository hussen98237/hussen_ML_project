# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=False
ALLOWED_ORIGINS=["*"]

# Security
# Generate a strong API key (e.g., using `openssl rand -hex 32`)
API_KEY=change_me_to_a_secure_key
# SHA256 hash of the rf_model.joblib file. 
# Run train_model.py to generate the model and see its hash.
MODEL_HASH_SHA256=

# Paths
# Absolute or relative paths
MODEL_PATH=rf_model.joblib
DATA_PATH=../data/RF_shuffled_data.xlsx
METRICS_PATH=metrics.json
